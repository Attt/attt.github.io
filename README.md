# attt.github.io
blog

- theme
```bash
git submodule init
git submodule update
```

- new
```bash
hexo new post 'new post name'
```

- generate
```bash
hexo clean && hexo g
```

- preview
```bash
hexo s
```
[default preview site](http://localhost:4000)
